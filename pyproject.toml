[project]
name = "cinemacervices"
version = "0.1.0"
description = ""
authors = [
    {name = "mialkin",email = "mialkin@bolid.ru"}
]
requires-python = ">=3.12"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
admin_panel = [
    "django (>=6.0.6,<7.0.0)",
    "pydantic-settings (>=2.14.1,<3.0.0)",
    "django-debug-toolbar (>=6.3.0,<7.0.0)",
    "psycopg2-binary (>=2.9.12,<3.0.0)"
]
dev = [
    "flake8 (>=7.3.0,<8.0.0)"
]
